{
  "name": "kodekur-e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for Kodekur e-commerce site",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
